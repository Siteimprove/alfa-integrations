# Alfa Angular

Alfa integration for Angular –turning Angular components into Alfa pages.

## Tests

Due to Angular need for specific setup to mount its components, the test live in the `@siteimprove/alfa-angular-test` package, which is structured as a minimal Angular library.
